{% extends 'base.html.twig' %}

{% block title %}Página Inicial{% endblock %}

{% block body %}
<div class="container">
    <div class="row align-items-center mb-5">
        <div class="col-lg-8">
            <h1 class="display-4 fw-bold mb-3">Bem-vindo ao Inpeace</h1>
            <p class="lead text-muted">Gerenciamento simples e eficiente de igrejas e membros da comunidade.</p>
        </div>
        <div class="col-lg-4 text-center">
            <i class="bi bi-building display-1 text-primary"></i>
        </div>
    </div>

    <div class="row g-4 mb-5">
        <div class="col-md-6">
            <div class="card h-100 border-0 shadow-sm hover-lift">
                <div class="card-body text-center py-5">
                    <div class="mb-3">
                        <i class="bi bi-plus-circle display-4 text-primary"></i>
                    </div>
                    <h5 class="card-title fw-bold mb-3">Criar Igreja</h5>
                    <p class="card-text text-muted mb-4">Registre uma nova igreja no sistema com endereço, website e imagem.</p>
                    <a href="{{ path('app_church_new') }}" class="btn btn-primary btn-lg">
                        <i class="bi bi-plus-lg"></i> Nova Igreja
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card h-100 border-0 shadow-sm hover-lift">
                <div class="card-body text-center py-5">
                    <div class="mb-3">
                        <i class="bi bi-person-plus display-4 text-success"></i>
                    </div>
                    <h5 class="card-title fw-bold mb-3">Criar Membro</h5>
                    <p class="card-text text-muted mb-4">Adicione um novo membro à comunidade com seus dados pessoais.</p>
                    <a href="{{ path('app_member_new') }}" class="btn btn-success btn-lg">
                        <i class="bi bi-plus-lg"></i> Novo Membro
                    </a>
                </div>
            </div>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-md-6">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body">
                    <h6 class="card-title fw-bold mb-3">
                        <i class="bi bi-church text-primary"></i> Gerenciar Igrejas
                    </h6>
                    <p class="card-text text-muted small mb-3">Visualize, edite e exclua igrejas cadastradas.</p>
                    <a href="{{ path('app_church_index') }}" class="btn btn-outline-primary btn-sm">
                        Ver Lista
                    </a>
                </div>
            </div>
        </div>

        <div class="col-md-6">
            <div class="card h-100 border-0 shadow-sm">
                <div class="card-body">
                    <h6 class="card-title fw-bold mb-3">
                        <i class="bi bi-people text-success"></i> Gerenciar Membros
                    </h6>
                    <p class="card-text text-muted small mb-3">Consulte, atualize e organize todos os membros.</p>
                    <a href="{{ path('app_member_index') }}" class="btn btn-outline-success btn-sm">
                        Ver Lista
                    </a>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
