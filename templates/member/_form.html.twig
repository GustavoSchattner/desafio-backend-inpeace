{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">{{ button_label|default('Salvar') }}</button>
{{ form_end(form) }}

<script src="{{ asset('js/cpf-mask.js') }}"></script>
<script src="{{ asset('js/ibge-service.js') }}"></script>